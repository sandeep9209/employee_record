<h1>Listing employees</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Age</th>
      <th>Salary</th>
      <th>Department</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.name %></td>
        <td><%= employee.email %></td>
        <td><%= employee.age %></td>
        <td><%= employee.salary %></td>
        <td><%= employee.department.name %></td>
	
        <td><%= link_to 'Show', employee %></td>
        <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
        <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		
		
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'orderbyage', orderbyage_path %>
<%= link_to 'orderbydepartment', orderbydepartment_path %>
<%= link_to 'orderbysalary', orderbysalary_path %>
<%= link_to 'retiredfromcompany', retiredfromcompany_path %>

<%= link_to 'highestsalary', highestsalary_path %>
<%= link_to 'top5salary', top5salary_path %>
<%= link_to 'seniorbyage', seniorbyage_path %>